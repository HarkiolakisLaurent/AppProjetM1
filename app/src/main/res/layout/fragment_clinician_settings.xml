<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="ceri.m1ilsen.applicationprojetm1.fragment.ClinicianSettingsFragment">

    <!-- TODO: Update blank fragment layout -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <Button
            android:id="@+id/editProfileButton"
            android:layout_width="1000dp"
            android:layout_height="100dp"
            android:layout_above="@+id/configureExerciseButton"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="53dp"
            android:background="@drawable/custom_list"
            android:fontFamily="monospace"
            android:text="Consulter profil"
            android:textColor="@android:color/background_light"
            android:textSize="24sp" />

        <Button
            android:id="@+id/configureExerciseButton"
            android:layout_width="1000dp"
            android:layout_height="100dp"
            android:layout_alignLeft="@+id/editProfileButton"
            android:layout_alignStart="@+id/editProfileButton"
            android:layout_centerVertical="true"
            android:background="@drawable/custom_list"
            android:fontFamily="monospace"
            android:text="Configurer exercice"
            android:textColor="@android:color/background_light"
            android:textSize="24sp" />

        <Button
            android:id="@+id/configureAlignmentButton"
            android:layout_width="1000dp"
            android:layout_height="100dp"
            android:layout_alignLeft="@+id/configureExerciseButton"
            android:layout_alignStart="@+id/configureExerciseButton"
            android:layout_below="@+id/configureExerciseButton"
            android:layout_marginTop="56dp"
            android:background="@drawable/custom_list"
            android:fontFamily="monospace"
            android:text="Configurer alignement"
            android:textColor="@android:color/background_light"
            android:textSize="24sp" />
    </RelativeLayout>

</FrameLayout>
